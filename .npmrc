# Ensure workspace dependencies are properly hoisted
legacy-peer-deps=true
auto-install-peers=true

# Use hoisted node-linker to avoid symlink issues with Docker volumes
# This installs all dependencies in the root node_modules
node-linker=hoisted
